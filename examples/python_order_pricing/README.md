# Python Order Pricing Strict Task

This task is a slightly more complex lightweight SWE-style benchmark. The order
pricing function mishandles coupon normalization, VIP stacking, free shipping
thresholds, and tax rounding.

The task uses:

- baseline bug reproduction
- fail-to-pass public bug tests
- pass-to-pass regression tests
- hidden edge tests

